import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gosoftonline.galapagosreisplan',
  appName: 'GalapagosReislan',
  webDir: 'out'
};

export default config;
